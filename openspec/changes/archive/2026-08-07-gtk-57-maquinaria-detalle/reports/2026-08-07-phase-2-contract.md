# Fase 2 — Contrato omitido — gtk-57-maquinaria-detalle

- Fecha: 2026-08-07
- Motivo: el change no añade Route Handlers, Server Actions ni mutaciones. Solo lectura pública RSC con `getPublishedMachineryBySlug()`.
- Schemas Zod existentes (`machinery-in-situ-tests`, `teamMachinerySeoSchema`) sin cambios de contrato.
- `api-spec.yml`: sin delta.

**Estado:** Omitida por diseño (aprobado en Gate 1).
