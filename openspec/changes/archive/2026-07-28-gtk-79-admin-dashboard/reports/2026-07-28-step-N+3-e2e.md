# Paso N+3 — E2E (GTK-79)

**Fecha:** 2026-07-28

Suite: `tests/e2e/gtk-79-admin-dashboard.spec.ts`

Cobertura prevista:

- Admin post-login ve dashboard y KPI CRM.
- Técnico no ve «Borradores IA».
- Click KPI → `/admin/proyectos`.
- Meta `robots` incluye `noindex`.

**Ejecución local:** requiere `DATABASE_URL`, `TWOFA_ENCRYPTION_KEY` y seed `tests/e2e/helpers/seed-gtk69-users.ts` (mismo prerequisito que GTK-68/69).

```text
pnpm exec playwright test tests/e2e/gtk-79-admin-dashboard.spec.ts
```

(Ejecutar en CI/entorno con Neon de pruebas antes del merge.)
