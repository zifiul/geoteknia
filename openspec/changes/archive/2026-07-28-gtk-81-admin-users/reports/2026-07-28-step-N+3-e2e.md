# E2E — gtk-81-admin-users

**Fecha:** 2026-07-28

Spec: `tests/e2e/gtk-81-admin-users.spec.ts`

- Admin con 2FA accede a `/admin/usuarios` y ve listado + CTA nuevo usuario.
- Técnico redirigido a `/admin/forbidden` al abrir `/admin/usuarios`.

Ejecución local: requiere `DATABASE_URL`, `TWOFA_ENCRYPTION_KEY` y servidor dev (`pnpm dev`) según configuración Playwright del repo.
