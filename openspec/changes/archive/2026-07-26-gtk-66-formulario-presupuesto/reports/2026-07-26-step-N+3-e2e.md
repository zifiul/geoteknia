# QA — E2E Playwright (GTK-66)

**Fecha:** 2026-07-26

## Ejecución

`CI=true pnpm run build && pnpm run test:e2e -- tests/e2e/gtk-66-formulario-presupuesto.spec.ts`

## Resultado

- 2 passed: metadata/StepIndicator, flujo feliz con mock `POST /api/leads/presupuesto` → `/gracias/presupuesto?ref=`.
- 2 skipped: catálogo servicio/provincia insuficiente en BD local (condición documentada en spec).

## Notas

- Turnstile mockeado vía `window.turnstile` (mismo patrón GTK-58).
- Tras `pnpm run build`, usar `CI=true` para evitar `reuseExistingServer` con build antiguo en puerto 3010.
