# Paso N+3 — gtk-67-click-to-call-whatsapp

- Spec: `tests/e2e/gtk-67-click-to-call-whatsapp.spec.ts`
- Comando: `npm run build && npm run test:e2e -- tests/e2e/gtk-67-click-to-call-whatsapp.spec.ts`
- Resultado: 3 tests **skipped** en entorno local sin servicios publicados / canal licitaciones en BD de `next start` (mismo patrón que GTK-49). Con seed Neon completo, los tests ejecutan aserciones de `tel:`, `wa.me?text=`, `click_tel` con `serviceSlug` y `click_email`.
