# Paso N+3 — E2E — gtk-58-licitaciones (2026-07-26)

## Comando

```
pnpm run build
pnpm exec playwright test tests/e2e/gtk-58-licitaciones.spec.ts
```

## Resultado

- 4/4 tests OK.
- Cobertura: 200 + JSON-LD, envío mock 201 → `/gracias/licitacion?ref=`, validación sin expediente/plataforma, enlace acreditaciones en hero.

## Notas

- Turnstile stub vía `addInitScript` + bloqueo de script Cloudflare en tests.
- Route mock confirma URL `/api/leads/licitacion`.
