# Fase 2 — Contrato omitida (GTK-63)

**Motivo:** la US no crea ni modifica Route Handlers, Server Actions ni schemas Zod de API.

**Contrato existente consumido:** salida de `create-*-lead` (`referenceNumber`, `downloadUrl` para recurso) y `THANK_YOU_PAGE_ROBOTS` (GTK-78).

**Siguiente fase:** TDD-RED.
