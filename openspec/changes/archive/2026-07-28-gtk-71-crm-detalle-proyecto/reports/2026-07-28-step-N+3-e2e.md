# Step N+3 — E2E Playwright

Suite: `tests/e2e/gtk-71-crm-detalle.spec.ts`

Ejecutar en entorno con `DATABASE_URL` y `TWOFA_ENCRYPTION_KEY`:

```text
pnpm exec playwright test tests/e2e/gtk-71-crm-detalle.spec.ts
```

Cubre: detalle admin desde lista, `noindex`, técnico sin asignación ni botones Eliminar.

**Ejecución local (2026-07-28):** falló — Neon inalcanzable + Playwright browsers no instalados en el entorno. Reintentar con BD activa y `pnpm exec playwright install`.
