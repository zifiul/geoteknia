# Docs — gtk-29-lead-ubicacion-api (fase 7)

- Fecha: 2026-07-24

## Actualizado

- `docs/technical/api-spec.yml`: path `POST /api/leads/ubicacion`, componente `LocationLeadInput`, tabla `x-geoteknia-authz`.

## Sin cambios requeridos

- `data-model.md`: enums/columnas ya documentados (GTK-12).
- `backend-standards.md`: patrón alineado con GTK-28 (subruta por tipo de lead).

## Trazabilidad

- Linear GTK-29 ↔ change `gtk-29-lead-ubicacion-api` ↔ delta specs en `openspec/changes/.../specs/`.
