# E2E — GTK-51

```
CI=1 npx playwright test tests/e2e/gtk-51-geo-landing.spec.ts
```

Resultado: 2 passed, 2 skipped (sin geo-zonas publicadas en BD de prueba), 0 failed.

Prerrequisito: `npm run build` para incluir rutas `/zonas` en el artefacto servido por `next start`.
