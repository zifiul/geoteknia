# Paso N+3 — gtk-57-maquinaria-listing (2026-07-26)

## Playwright

```
pnpm run build
pnpm exec playwright test tests/e2e/gtk-57-maquinaria-listing.spec.ts --project=chromium
```

- 3 passed: 200 + breadcrumb JSON-LD, fichas/specs/enlaces (o vacío), smoke estado vacío.

## Notas

- Label `Frontend`: E2E obligatorio (no omitido).
- Tras `pnpm run build`, `/maquinaria` aparece como SSG `revalidate 3600`.
