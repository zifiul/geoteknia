# Paso N+3 — E2E Playwright (GTK-76)

**Fecha:** 2026-07-27

- `pnpm run test:e2e:a11y`: **11 passed** (9 plantillas + catálogo GTK-44 + teclado presupuesto).
- Helper: `tests/e2e/helpers/axe-wcag.ts`.
- Requiere `pnpm build` previo cuando el webServer usa `next start`.
