# Paso N+3 — E2E Playwright (GTK-62)

**Fecha:** 2026-07-26

## Comando

`npm run build && CI=true npm run test:e2e -- tests/e2e/gtk-62-faqs-schema-faqpage.spec.ts`

## Resultado

- 2 passed: `/faqs` 200 + breadcrumb JSON-LD; acordeón en DOM/teclado.
- 2 skipped: sin grupos FAQ o servicios con FAQs en el entorno E2E (condicional en spec).

## Notas

- Requiere `next build` antes de E2E (`webServer` usa `next start`).
