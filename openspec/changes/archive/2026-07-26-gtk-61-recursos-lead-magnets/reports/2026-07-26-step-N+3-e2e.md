# Paso N+3 — E2E (GTK-61)

- Spec: `tests/e2e/gtk-61-recursos-lead-magnets.spec.ts`
- Ejecución: `CI=true npm run test:e2e -- tests/e2e/gtk-61-recursos-lead-magnets.spec.ts` → 3 passed, 1 skipped (sin recursos en catálogo para flujo de envío).
- Nota: con `reuseExistingServer` local hace falta `npm run build` antes del E2E si `next start` estaba en caché sin `/recursos`.
