# Fase 2 — Contrato (GTK-61)

Actualizado `docs/technical/api-spec.yml` con `GET /api/recursos/download` (authz público, sin Turnstile, respuestas 302/400/404).

`resourceLeadSchema` sin cambios (GTK-30).
