# Curl / smoke HTTP — gtk-47-layout-publico

- **Fecha:** 2026-07-25
- **GET /** — 200, layout con `header`/`main`/`footer` (verificado vía E2E).
- **GET /dev-seo** — 200, breadcrumbs visibles + JSON-LD `BreadcrumbList`.
- **GET /contacto** — 200, placeholder hasta GTK-60.
- Sin API nueva en el change.
