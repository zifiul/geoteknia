# E2E GTK-72 (pendiente ejecución local/CI)

Spec: `tests/e2e/gtk-72-cms-listado.spec.ts`

Casos:

- Editor filtra por `borrador_ia` y ve tabla
- Editor ve menú crear
- Técnico → `/admin/forbidden` en `/contenido`
- Acceso rápido dashboard → `/contenido`
- Meta `noindex`

Ejecutar con seed GTK-69 y credenciales E2E documentadas en el spec.
