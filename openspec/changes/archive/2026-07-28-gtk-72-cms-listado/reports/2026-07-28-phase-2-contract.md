# Fase 2 — Contrato gtk-72-cms-listado

**Omitido parcialmente:** sin Route Handlers ni mutaciones nuevas.

**Congelado:**

- `lib/admin/cms-filters-schema.ts` — filtros URL (`type`, `status`, `silo`, `page`, `pageSize`)
- Catálogo presentación `lib/admin/cms-content-types.ts`

`docs/technical/api-spec.yml` sin cambios (Server Actions/listado server-only existente).
