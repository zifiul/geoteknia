# Paso N+3 — E2E Playwright (GTK-64)

**Fecha:** 2026-07-27

## Comando

```bash
pnpm run build
pnpm exec playwright test tests/e2e/gtk-64-calculadora-widget.spec.ts
```

## Resultado

6/6 passed (chromium, baseURL `http://localhost:3010`).

Cubre: 200 + JSON-LD, cálculo feliz sin precio + CTA presupuesto, bloqueo inválido, 422 orientativo, ausencia de mirror `calculator_use`, `servicio` en CTA solo con query.
