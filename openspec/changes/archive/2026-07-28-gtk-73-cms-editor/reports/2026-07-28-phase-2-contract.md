# Fase 2 — Contrato gtk-73-cms-editor

**Omitida.** El change no añade Route Handlers ni Server Actions de mutación; reutiliza `app/(admin)/(portal)/contenido/actions.ts` (GTK-41) y schemas Zod existentes (`seoBlockSchema`, `createServiceSchema` en servidor).
