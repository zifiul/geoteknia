# Paso N+3 — E2E Playwright

Fecha: 2026-07-27

- Spec: `tests/e2e/gtk-65-location-widget.spec.ts`
- `npm run build` + `npm run test:e2e -- tests/e2e/gtk-65-location-widget.spec.ts`
- Resultado local: **4 skipped** (sin enlaces a servicios en `/servicios` — BD sin seed en entorno E2E local). Mismo patrón que GTK-49 (`test.skip` si no hay servicios).
- Con BD sembrada (CI/Neon), los casos ejecutan mock de Turnstile + `POST /api/leads/ubicacion` stub.
