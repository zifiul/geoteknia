# E2E GTK-52 — intersección servicio+zona

- Fecha: 2026-07-26
- Comando: `npm run test:e2e -- tests/e2e/gtk-52-intersection-page.spec.ts`

## Resultados

| Escenario | Resultado |
|-----------|-----------|
| Combinación inexistente → 404 | PASS |
| JSON-LD Service + canonical + no-canibalización | SKIP (sin `service_zone_pages` publicadas en BD de test) |
| CTA presupuesto servicio+provincia | SKIP (misma razón) |

## Notas

Patrón GTK-49/GTK-51: los tests que requieren contenido CMS publicado se omiten cuando la BD no tiene filas. Tras seed editorial, re-ejecutar la suite para cobertura completa.

## Resultado

PASS con skips documentados; no bloqueante para merge si el entorno de CI tiene intersecciones publicadas.
